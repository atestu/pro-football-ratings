[project]
name = "pro-football-ratings"
version = "1.0.0"
description = "Track NFL expert picks, compare against actual results, and rate pickers against a Vegas baseline"
requires-python = ">=3.12"
dependencies = [
    "nflreadpy>=0.1.5",
    "polars>=1.0.0",
]
